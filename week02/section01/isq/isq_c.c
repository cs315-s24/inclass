#include <stdbool.h>

bool isq_c(char ch) {
    bool isq = false;
    if (ch == 'q') {
        isq = true;
    }
    return isq;
    
    // if (ch == 'q') {
    //     return true;
    // } else {
    //     return false;
    // }
}
